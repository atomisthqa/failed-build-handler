export function email(results: any[]) {
    setTimeout(() => {
        results.forEach(r => {
            const email = r.data.build.push.head.author.email;
            console.log(`sent email to ${email}`);
        });
    }, 3000);
}
